---
applications:
- name: accounts
  random-route: true
  timeout: 180
  instances: 1
  memory: 1G
  path: libs/accounts-${version}.jar
  services: [ traderdb, discovery-service, circuit-breaker-dashboard, config-server ]
